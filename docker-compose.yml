version: '3.8'

services:
  web-app:
    build: .
    ports:
      - "5000:5000"
    networks:
      - my-network

  proxy:
    image: nginx:alpine
    ports:
      - "80:80"
    networks:
      - my-network

networks:
  my-network:
    driver: bridge
